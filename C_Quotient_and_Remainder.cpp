#include<bits/stdc++.h>
using namespace std;

void solve(){
    int n, k;
    cin >> n >> k;
    vector<int>q(n);
    vector<int>r(n);
    for(int i=0; i<n; i++){
        cin >> q[i];
    }
    for(int i=0; i<n; i++){
        cin >> r[i];
    }

    
}

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;
    cin >> t;
    while(t--){
        solve();
    }
    return 0;
}